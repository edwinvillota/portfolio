import styled from 'styled-components'

export const LogoText = styled.h1`
  font-family: ${props => props.theme.fonts.logo};
  font-size: ${props => props.theme.fontSizes.mobLogo};
  font-weight: 400;
`
